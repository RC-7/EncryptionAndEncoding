PublicKeys.txt has all key information for users for simplicity.

"errorLog.txt" gives information of all errors and attacks identified.

"Ledger.txt" contains the initial ledger for the system. This is not changed after the simulation to allow for multiple runs.

Running the "Testing.m" file will run the appropriate simulation of the system and call all necessary functions